{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "author": "marianyfs",
  "scripts": {
    "start": "json-server --watch src/db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
